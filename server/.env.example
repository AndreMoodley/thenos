# VOIDBORN server environment. Copy to .env and fill in. JWT_SECRET MUST be set before boot.
DATABASE_URL="postgresql://postgres:musashi@localhost:5432/postgres?schema=public&sslmode=disable"
SHADOW_DATABASE_URL="postgresql://postgres:musashi@localhost:5432/postgres?schema=public&sslmode=disable"
JWT_SECRET="U1fpLPrT19mQ51K73G6zJN3bQyhLuRUy4zdjuxeY7Mn"
JWT_EXPIRES_IN="7d"
# Optional — the Voice of the Void falls back to a local, data-grounded reflection when unset.
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-opus-4-8"
PORT=4000
# Comma-separated allowlist for CORS in production; empty = reflect origin (dev).
CORS_ORIGINS=""
